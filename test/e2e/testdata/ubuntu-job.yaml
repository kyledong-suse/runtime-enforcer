apiVersion: batch/v1
kind: Job
metadata:
  name: ubuntu-job
  namespace: default
spec:
  template:
    spec:
      containers:
      - name: ubuntu
        image: ubuntu
        command: ["bash", "-c", "while true; do sleep 5; ls; done"]
      restartPolicy: OnFailure
